using System.Collections.Generic;

namespace ClusterizerLib.DbScan;

/// <summary>
/// Run a Db Scan cluster algorithm
/// </summary>
public static class ClusterizerDbScan
{
    /// <summary>
    /// Run the cluster on given points
    /// </summary>
    public static IReadOnlyList<ClusterResult> Run(IReadOnlyList<IPoint> points)
    {
        

        return new List<ClusterResult>();
    }
}